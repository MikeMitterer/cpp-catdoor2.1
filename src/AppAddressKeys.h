#pragma once
#include "AppAddress.h"

namespace mm::AppAddressKey {
    static const AppAddress useSensor1(AppAddress::Key::useSensor1);
    static const AppAddress useSensor2(AppAddress::Key::useSensor2);
    static const AppAddress reactOnSensor(AppAddress::Key::reactOnSensor);
    static const AppAddress lastSSID(AppAddress::Key::lastSSID);
}